---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="date-cart" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
